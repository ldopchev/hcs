# install all node modules in client and server dir with yarn install
# node will be looking for mongo to run on localhost:27017
# make a POST request to http://localhost:3001/api/register to create a user: 
{	
		"username": "user",
		"password": "pass1",
		"firstName": "John",
		"lastName": "Doe",
		"email": "john.doe@gmail.com"
	
}

# start the application with yarn start in both directories